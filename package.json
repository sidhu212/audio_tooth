{
  "name": "audio-tooth-signaling-server",
  "version": "1.0.0",
  "description": "A production-ready signaling server for WebRTC audio streaming.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
